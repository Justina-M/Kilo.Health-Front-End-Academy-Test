import { ReactComponent as Logo } from "../../../assets/logo.svg";

const Header = () => {
  return (
    <header className="Header">
      <a href="#">
        <Logo />
      </a>
    </header>
  );
};

export default Header;
